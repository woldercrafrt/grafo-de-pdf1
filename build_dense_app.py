import json
import os

# We will load the nodes and build the interactive HTML with high density and toggleable related nodes.
# Let's inspect the nodes we have defined.
print("Script build_dense_app.py initialized.")
